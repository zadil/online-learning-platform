version: "2"

sql:
  - engine: postgresql
    queries: "queries/"
    schema: "deploy/"
    gen:
      go:
        package: "db"
        out: "internal/db"
        emit_json_tags: true
        emit_prepared_queries: true
        emit_interface: false
        emit_exact_table_names: false

# Pour lancer : sqlc generate
